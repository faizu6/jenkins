pipeline {
    agent { label 'prod-slave' } 
    stages {
        stage('run tasks') {
            steps {
                sh '''
                  pwd
                   '''
            }
        }
    }
}
